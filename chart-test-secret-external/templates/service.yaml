apiVersion: v1
kind: Service
metadata:
  name: {{ include "chart-test-secret-external.fullname" . }}
  labels:
    {{- include "chart-test-secret-external.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chart-test-secret-external.selectorLabels" . | nindent 4 }}
